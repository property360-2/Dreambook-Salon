<!-- documentation\METRIC_CALCULATION_PROOF.md -->
# Metric Calculation Proof and Implementation

This document provides the mathematical formulas and Python implementation details for the metrics used in Chapter 4.

## 1. Regression Metrics (Amount Spent)

The regression model predicts the continuous value of customer spending.

### Mathematical Formulas

*   **Mean Absolute Error (MAE):**
    $$MAE = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|$$
    *Where $y_i$ is the actual value and $\hat{y}_i$ is the predicted value.*

*   **Root Mean Square Error (RMSE):**
    $$RMSE = \sqrt{\frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2}$$

*   **Coefficient of Determination ($R^2$):**
    $$R^2 = 1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2}$$
    *Where $\bar{y}$ is the mean of the actual values.*

### Python Implementation (scikit-learn)

```python
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score
import numpy as np

# Sample Data (Actual vs Predicted)
y_actual = [1200, 850, 3100, 500, 1500]
y_pred = [1100, 900, 2800, 600, 1450]

mae = mean_absolute_error(y_actual, y_pred)
rmse = np.sqrt(mean_squared_error(y_actual, y_pred))
r2 = r2_score(y_actual, y_pred)

print(f"MAE: {mae}")
print(f"RMSE: {rmse}")
print(f"R2 Score: {r2}")
```

---

## 2. Classification Metrics (Customer Retention)

The classification model predicts a binary outcome (1 = Return, 0 = No Return).

### Mathematical Formulas

*   **Accuracy:**
    $$Accuracy = \frac{TP + TN}{TP + TN + FP + FN}$$
    *TP: True Positive, TN: True Negative, FP: False Positive, FN: False Negative.*

*   **Precision:**
    $$Precision = \frac{TP}{TP + FP}$$

*   **Recall (Sensitivity):**
    $$Recall = \frac{TP}{TP + FN}$$

*   **F1-Score:**
    $$F1 = 2 \times \frac{Precision \times Recall}{Precision + Recall}$$

### Python Implementation (scikit-learn)

```python
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score

# Sample Data (Actual vs Predicted)
# 1: Customer Returned, 0: Customer Did Not Return
y_true = [1, 0, 1, 1, 0, 1, 0, 1]
y_pred = [1, 0, 1, 0, 0, 1, 1, 1]

accuracy = accuracy_score(y_true, y_pred)
precision = precision_score(y_true, y_pred)
recall = recall_score(y_true, y_pred)
f1 = f1_score(y_true, y_pred)

print(f"Accuracy: {accuracy}")
print(f"Precision: {precision}")
print(f"Recall: {recall}")
print(f"F1 Score: {f1}")
```

## Summary of Results
The metrics reported in Chapter 4 were generated by applying these standard statistical methods to the salon's validation dataset. The values (e.g., $R^2 = 0.82$) indicate the performance of the model on unseen data, providing a measure of how well the system will generalize to future salon customers.
